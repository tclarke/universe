{
  "type": "object",
  "description": "Properties of Weave Scope package",
  "properties": {
    "weavescope": {
      "type": "object",
      "description": "Weave Scope consists of two components - the probe for collecting container host metrics and the app for UI",
      "properties": {
        "probe": {
          "type": "object",
          "properties": {
            "instances": {
              "type": "integer",
              "description": "Number of Scope probes to deploy, it should be equals to number of Mesos slaves",
              "default": 1
            }
          }
        },
        "app": {
          "type": "object",
          "properties": {
            "instances": {
              "type": "integer",
              "description": "Number of Scope UI apps to deploy, it should be more then one, unless you are using hosted Scope UI service and have set `weavescope.service.token`",
              "default": 1
            }
          }
        },
        "service": {
          "type": "object",
          "description": "You can sign-up to hosted Scope UI service at https://scope.weave.works and use it to connect probes in different environment and have permanent access to the UI, without exposing ports",
          "properties": {
            "token": {
              "type": "string",
              "description": "You can set your Scope service access token here"
            }
          }
        }
      }
    }
  }
}
